---
epic: 6
story: "6.2"
title: Declarar dependências e garantir instalação limpa reproduzível
status: InReview
executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: [dependency_audit, clean_install_test]
sequence:
  order: 1
  of: 5
  recommended_by: "@pm (docs/prd/README.md §3)"
  rationale: >
    Story de menor esforço e maior efeito de desbloqueio: uma instalação limpa
    quebrada bloqueia qualquer outra story, inclusive a criação da baseline
    de testes (6.3), cujo smoke test de import depende dos pacotes declarados aqui.
dependencies:
  depends_on: []
  blocks:
    - "6.3 (smoke test de import de src/core/deduplicator.py falha com ImportError sem numpy/scikit-learn/rapidfuzz declarados)"
    - "1.1, 1.2, 1.3 (ambiente de desenvolvimento limpo é pré-requisito indireto de todo o Epic 1)"
---

# Story 6.2: Declarar dependências e garantir instalação limpa reproduzível

## Status

InReview

## Story

**As a** consultor/desenvolvedor configurando o ambiente do Data Catalog & Profiler em uma máquina nova (clone limpo do repositório),
**I want** que o manifesto de dependências declare todos os pacotes efetivamente importados pelo código e que a relação entre os dois manifestos existentes seja resolvida sem ambiguidade,
**so that** `pip install -r <manifesto>` seguido de `streamlit run` funcione sem nenhum `ImportError`, desbloqueando o restante do roadmap (Epic 1 e a baseline de testes da Story 6.3).

## Dependencies

- **Depende de:** nenhuma story (primeira da sequência recomendada pelo @pm).
- **Bloqueia:** Story 6.3 — o smoke test de import de todos os módulos de `src/` (6.3 AC4) importa `src/core/deduplicator.py`, que hoje importa `numpy`, `sklearn` e `rapidfuzz` sem que nenhum dos três esteja declarado em `requirements_catalog.txt`; sem esta story, o smoke test falha por ambiente, não por bug.
- **Ordem de execução recomendada pelo @pm** (`docs/prd/README.md` §3): 6.2 → 6.3 → 1.1 → 1.2 → 1.3. Esta é a story 1 de 5 dessa sequência.

## Acceptance Criteria

Copiado literalmente de `docs/prd/epic-6-debito-tecnico-plataforma.md` (Story 6.2):

1. `requirements_catalog.txt` passa a declarar **todas** as dependências efetivamente importadas pelo código, incluindo `numpy`, `scikit-learn` e `rapidfuzz` (`deduplicator.py:1-5`), com versões mínimas.
2. Auditoria completa: todo import de terceiros em `src/` é cruzado com o manifesto; a lista de discrepâncias é reportada e zerada.
3. A relação entre `requirements.txt` e `requirements_catalog.txt` é resolvida — consolidação em um único manifesto ou documentação explícita de qual serve a quê. Ambiguidade de manifesto não permanece.
4. **Teste de instalação limpa:** em virtualenv novo, `pip install -r <manifesto>` seguido de `streamlit run` permite navegar nas duas telas (Profiling e Duplicidades) sem nenhum `ImportError`. O procedimento é documentado e reproduzido por outra pessoa que não a autora.
5. Instruções de setup do ambiente documentadas (versão de Python suportada, criação de venv, execução).
6. Nenhuma dependência é adicionada sem uso real no código (o inverso do débito atual).

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
**Primary Type**: Deployment/Infrastructure
**Secondary Type(s)**: N/A
**Complexity**: Low — mudança confinada a manifestos de dependência e documentação, sem alteração de código de aplicação.

**Specialized Agent Assignment:**
**Primary Agents**:
- @devops (executor — auditoria de dependências, manifesto, teste de instalação limpa)
- @dev (pre-commit review, conforme padrão de todas as stories)

**Supporting Agents**:
- @architect (quality gate — revisão final de auditoria de dependências e teste de instalação limpa, conforme definido no epic)

**Quality Gate Tasks:**
- [ ] Pre-Commit (@devops): Rodar `coderabbit --prompt-only -t uncommitted` antes de marcar a story como completa
- [ ] Pre-PR (@devops): Rodar `coderabbit --prompt-only --base main` antes de criar o pull request (exclusivo de @devops por `agent-authority.md`)
- [ ] Pre-Deployment: N/A — story não afeta produção, apenas ambiente de desenvolvimento/instalação

**Self-Healing Configuration:**
**Expected Self-Healing**:
- Primary Agent: @devops (modo *check* — auditoria/relatório, sem auto-fix de código de aplicação, análogo ao mapeamento `@github-devops` em `.claude/rules/coderabbit-integration.md`)
- Max Iterations: N/A (modo check = report_only)
- Timeout: N/A
- Severity Filter: report_only

**Predicted Behavior**:
- CRITICAL issues: report_only (auditoria de dependência não faz auto-fix de código; discrepâncias reportadas para ação manual do @devops)
- HIGH issues: report_only

**CodeRabbit Focus Areas:**
**Primary Focus**:
- Nenhuma dependência declarada sem uso real no código (AC6)
- Versões mínimas coerentes com o que está instalado/testado (AC1)

**Secondary Focus**:
- Ambiguidade entre `requirements.txt` e `requirements_catalog.txt` resolvida sem resíduo (AC3)
- Documentação de setup clara e reproduzível por terceiros (AC5)

## Tasks / Subtasks

- [x] Task 1 — Auditoria de imports de terceiros em `src/` (AC: 1, 2)
  - [x] Listar todos os imports de terceiros em `src/core/`, `src/models/`, `src/validators/`, `src/app.py`
  - [x] Confirmar achado já verificado: `src/core/deduplicator.py:1-5` importa `pandas`, `numpy`, `sklearn.feature_extraction.text.TfidfVectorizer`, `sklearn.metrics.pairwise.cosine_similarity`, `rapidfuzz.fuzz` — apenas `pandas` está no manifesto atual
  - [x] Confirmar que `src/validators/email_val.py:2` importa `dns.resolver` (pacote `dnspython`), já declarado
  - [x] Cruzar lista completa de imports com `requirements_catalog.txt` atual (`streamlit>=1.30.0`, `pandas>=2.0.0`, `sqlalchemy>=2.0.0`, `dnspython>=2.5.0`, `openpyxl>=3.1.0`)
  - [x] Produzir e anexar ao Dev Agent Record a lista de discrepâncias (esperado: `numpy`, `scikit-learn`, `rapidfuzz` ausentes)
- [x] Task 2 — Atualizar `requirements_catalog.txt` (AC: 1, 6)
  - [x] Adicionar `numpy`, `scikit-learn`, `rapidfuzz` com versões mínimas compatíveis com o restante do stack (Python + pandas 2.x)
  - [x] `rapidfuzz` **entra no manifesto normalmente** — é import morto em `deduplicator.py:5` (nunca chamado), mas é executado no carregamento do módulo; sem ele, `ImportError`. Ver Dev Notes → "RESOLVIDO — AC1 × AC6". Adicionar comentário no manifesto marcando-o como pendente de revisão na Story 6.1
  - [x] Escolher pisos de versão de `numpy`/`scikit-learn` que tenham wheel binária `win_amd64` para a versão de Python fixada na Task 5 (ver Dev Notes → risco de wheels binárias em Windows)
  - [x] Não adicionar nenhum pacote sem import real correspondente confirmado na Task 1
- [x] Task 3 — Resolver ambiguidade entre `requirements.txt` e `requirements_catalog.txt` (AC: 3)
  - [x] Nota de achado (verificado em 2026-09-09): `requirements.txt` hoje contém apenas `anthropic>=0.39.0` e `mcp>=1.1.0` — pacotes não importados em nenhum lugar de `src/`, aparentemente resíduo de outra ferramenta/projeto (tooling AIOX/MCP), não do app Data Catalog & Profiler
  - [x] Decidir e documentar: manter `requirements.txt` como manifesto de tooling não relacionado ao app (com comentário explicando o propósito) OU removê-lo/consolidar, se confirmado que não é usado por nenhum processo de CI/instalação do app
  - [x] Garantir que o README/instruções de setup apontem inequivocamente para `requirements_catalog.txt` como o manifesto do app
- [x] Task 4 — Teste de instalação limpa (AC: 4)
  - [x] Criar virtualenv novo isolado do ambiente atual
  - [x] `pip install -r requirements_catalog.txt`
  - [x] `streamlit run src/app.py` e navegar nas duas telas (Data Discovery & Profiling, Análise de Duplicidades) sem `ImportError`
  - [x] Documentar o procedimento passo a passo para reprodução por uma segunda pessoa
- [x] Task 5 — Documentar setup do ambiente (AC: 5)
  - [x] Versão de Python suportada
  - [x] Passos de criação de venv (Windows, ambiente do usuário)
  - [x] Comando de execução (`streamlit run src/app.py`)
- [x] Task 6 — Registrar evidência para o quality gate (@architect) (AC: 1-6)
  - [x] Relatório de auditoria de dependências (dependency_audit) anexado ao Dev Agent Record
  - [x] Evidência do teste de instalação limpa (clean_install_test) anexada ao Dev Agent Record

## Dev Notes

### Contexto e achados verificados no código (2026-09-09)

- `requirements_catalog.txt` (raiz do projeto) hoje contém exatamente:
  ```
  streamlit>=1.30.0
  pandas>=2.0.0
  sqlalchemy>=2.0.0
  dnspython>=2.5.0
  openpyxl>=3.1.0
  ```
  [Source: requirements_catalog.txt — lido diretamente, 2026-09-09]
- `requirements.txt` (raiz do projeto) hoje contém exatamente:
  ```
  anthropic>=0.39.0
  mcp>=1.1.0
  ```
  [Source: requirements.txt — lido diretamente, 2026-09-09]. Nenhum desses dois pacotes é importado em `src/`. Isso é consistente com a leitura do epic de que "existem dois manifestos... sem relação documentada entre eles" — a causa raiz aparente é que `requirements.txt` pertence a outra ferramenta (tooling de IA/MCP), não ao app Data Catalog & Profiler. Esta é uma inferência a confirmar/documentar pelo executor, não uma AC nova — a AC3 já exige resolver essa ambiguidade.
- `src/core/deduplicator.py:1-5`:
  ```python
  import pandas as pd
  import numpy as np
  from sklearn.feature_extraction.text import TfidfVectorizer
  from sklearn.metrics.pairwise import cosine_similarity
  from rapidfuzz import fuzz
  ```
  [Source: src/core/deduplicator.py — lido diretamente, 2026-09-09]. `numpy`, `scikit-learn` (módulo `sklearn`) e `rapidfuzz` não constam no manifesto — confirma o débito descrito no epic.
- `src/validators/email_val.py:2` importa `dns.resolver` — já coberto por `dnspython>=2.5.0` no manifesto atual. Não é uma discrepância nova, apenas confirmação de que o pacote existente é usado de fato.

### RESOLVIDO — AC1 × AC6 sobre `rapidfuzz`: declarar normalmente (decisão do @po, 2026-09-09)

**Não há conflito entre a AC1 e a AC6. `rapidfuzz` DEVE ser declarado nesta story. Isto não é uma questão em aberto para o executor decidir.**

Achado verificado (re-confirmado em 2026-09-09, leitura integral de `src/core/deduplicator.py`): a linha 5 executa `from rapidfuzz import fuzz`, mas o símbolo `fuzz` **nunca é chamado** em nenhum ponto do arquivo — a lógica de similaridade de `find_duplicates` usa `TfidfVectorizer` + `cosine_similarity` (linhas 31-35) e `np.where` (linhas 49, 76). `rapidfuzz` é, portanto, um **import morto**. [Source: src/core/deduplicator.py — lido integralmente, 2026-09-09]

Resolução, com o critério explicitado para não deixar margem de interpretação:

- **A AC1 governa esta story.** Ela pede declarar o que está **efetivamente importado pelo código hoje**. Um import morto ainda é executado no carregamento do módulo: sem `rapidfuzz` instalado, `import src.core.deduplicator` levanta `ImportError` e a tela de Duplicidades não abre. Omitir o pacote quebraria a instalação limpa exigida pela AC4 e o smoke test da Story 6.3 (AC4 de lá).
- **"Uso real" na AC6 significa "é importado e necessário para o módulo carregar sem `ImportError`"** — não "toda função importada é de fato chamada downstream". A AC6 existe para impedir o inverso do débito atual (declarar pacote que o código nem importa, como `anthropic`/`mcp` em `requirements.txt`), não para transformar esta story em uma caçada a código morto.
- **Remover o import morto é escopo da Story 6.1** (débito de escalabilidade/qualidade do `deduplicator.py`), **não desta story.** Se e quando a 6.1 remover a linha 5, `rapidfuzz` poderá sair do manifesto — nesse momento, e não antes. Registrar essa dependência de escopo no Dev Agent Record para que o quality gate da 6.1 encontre o contexto.
- **Ação concreta para o executor:** declarar `rapidfuzz` na Task 2 junto com `numpy` e `scikit-learn`, e adicionar comentário no manifesto marcando-o como import de `deduplicator.py` pendente de revisão na Story 6.1. Nenhum AC precisa ser alterado; nenhuma decisão adicional é necessária.

### Risco de instalação: wheels binárias de `numpy`/`scikit-learn` em Windows

O ambiente-alvo deste projeto é **Windows** (Windows 11 Pro, ambiente do usuário — o mesmo onde o teste de instalação limpa da AC4 será executado). `numpy` e `scikit-learn` são pacotes com extensões compiladas em C/C++/Fortran e dependem de **wheel binária pré-compilada** para a combinação exata de versão de Python + plataforma. Implicações práticas para a Task 2 e a Task 4:

- Se a versão mínima escolhida não tiver wheel `win_amd64` para a versão de Python usada, o `pip` tentará compilar a partir do source e falhará (exige MSVC Build Tools / toolchain Fortran) — o sintoma aparecerá como falha de build, não como `ImportError`, e pode ser confundido com erro de manifesto.
- Ancorar a versão mínima do Python (AC5) **antes** de fixar os pisos de versão de `numpy`/`scikit-learn`, e escolher pisos que comprovadamente tenham wheel para essa combinação. `scikit-learn` puxa `numpy` e `scipy` transitivamente — deixar o resolver do pip harmonizar as versões em vez de fixar pisos agressivos demais.
- A Task 4 (instalação limpa em venv novo) é o ponto onde esse risco se materializa ou se descarta. Se o `pip install` falhar por build de wheel, isso é achado da AC4 e deve ser documentado no Dev Agent Record com a mensagem de erro completa — não contornado com instalação manual fora do manifesto, o que invalidaria a reprodutibilidade exigida pela AC4.

### Arquitetura / Padrões do Projeto

Não há documentação de arquitetura sharded (`docs/architecture/`) neste projeto ainda — apenas o PRD brownfield em `docs/prd/`. Nenhuma informação adicional de arquitetura disponível além do já citado nos epics. [Nota: docs/architecture/ não existe no momento desta story; não invente convenções não verificadas no código.]

### Testing

- Não há framework de testes configurado ainda neste projeto (isso é exatamente o objeto da Story 6.3, que vem depois desta na sequência recomendada).
- Para esta story, a validação é primariamente manual/procedural: teste de instalação limpa em venv novo (AC4) é o próprio "teste" desta story, documentado passo a passo para reprodução.
- Quando a Story 6.3 for concluída, o smoke test de import de todos os módulos de `src/` (6.3 AC4) passa a servir como regressão automatizada para o que esta story corrige — vale registrar essa relação no Dev Agent Record para o quality gate de 6.3 encontrar o contexto.

## Change Log

| Date | Version | Description | Author |
|------|---------|--------------|--------|
| 2026-09-09 | 0.1 | Rascunho inicial da story a partir do Epic 6 | River (@sm) |
| 2026-09-09 | 0.2 | Validated GO (9/10) — Status: Draft → Ready. Should-fix registrados (risco de roda binária scikit-learn/numpy em Windows; tensão AC1×AC6 sobre `rapidfuzz` importado e não usado — remoção é escopo da Story 6.1 AC4) | Pax (@po) |
| 2026-09-09 | 0.3 | **Should-fix da v0.2 ENCERRADOS — nenhuma pendência aberta nesta story.** (a) Tensão AC1×AC6 sobre `rapidfuzz` resolvida em Dev Notes (seção "RESOLVIDO — AC1 × AC6"): `rapidfuzz` É declarado nesta story, pois é import executado no carregamento do módulo (`deduplicator.py:5`); "uso real" na AC6 = "necessário para o módulo carregar sem ImportError"; remoção do import morto é escopo da Story 6.1. Texto dos ACs não alterado; subtask explícita adicionada à Task 2. (b) Risco de wheels binárias `numpy`/`scikit-learn` em Windows documentado em Dev Notes, com subtask de ancoragem de versão na Task 2 e tratamento de falha na Task 4 | Pax (@po) |
| 2026-09-09 | 0.4 | Development started (yolo mode) — Status: Ready → InProgress | @dev |
| 2026-09-09 | 0.5 | Development complete — Status: InProgress → InReview. Manifesto do app completo (numpy/scikit-learn/rapidfuzz declarados, piso de pandas elevado), `requirements.txt` renomeado para `requirements-aiox-tooling.txt` (AC3), README de setup criado (AC5) e teste de instalação limpa executado em venv novo com navegação nas duas telas (AC4). CI ajustado para Python 3.11 (piso de numpy) | @dev |

## Dev Agent Record

### Agent Model Used

Dex (@dev) — claude-opus-5, modo YOLO (autônomo). Executada por @dev e não por @devops (campo `executor` do frontmatter): a story não contém nenhuma operação exclusiva de @devops (`git push`, PR, MCP, CI/CD de produção). O `git push` permanece com @devops.

### Debug Log References

Evidências coletadas em 2026-09-09 (ambiente: Windows 11 Pro, Python 3.14.3 em `C:\Python314`):

1. **Auditoria de imports (dependency_audit)** — `grep -rnE "^\s*(import|from)\s+" src/`:

   | Pacote de terceiros | Onde é importado | Estado antes | Ação |
   |---|---|---|---|
   | `streamlit` | `src/app.py:1` | declarado | mantido |
   | `pandas` | `src/app.py:2`, `db_manager.py:2`, `profiler.py:1`, `deduplicator.py:1` | declarado (`>=2.0.0`) | piso elevado p/ `>=2.3.3` |
   | `sqlalchemy` | `src/core/db_manager.py:1` | declarado | mantido |
   | `openpyxl` | sem `import`; engine exigida em runtime por `pd.ExcelWriter(engine='openpyxl')` em `src/app.py:136,194` | declarado | mantido (uso real confirmado) |
   | `dnspython` (`dns.resolver`) | `src/validators/email_val.py:2` | declarado | mantido |
   | **`numpy`** | `src/core/deduplicator.py:2` | **AUSENTE** | adicionado `>=2.3.2` |
   | **`scikit-learn`** (`sklearn`) | `src/core/deduplicator.py:3-4` | **AUSENTE** | adicionado `>=1.7.2` |
   | **`rapidfuzz`** | `src/core/deduplicator.py:5` | **AUSENTE** | adicionado `>=3.14.0` |

   Stdlib (não entram em manifesto): `traceback`, `io`, `typing`, `sqlite3`, `json`, `re`, `string`.
   Discrepâncias encontradas: **3** (`numpy`, `scikit-learn`, `rapidfuzz`). Discrepâncias remanescentes após a correção: **0**.
   Pacote declarado sem uso real: **nenhum** no manifesto do app (AC6). `scipy`/`joblib`/`threadpoolctl` entram apenas como dependências transitivas de `scikit-learn` e por isso **não** são declarados.

2. **Prova do `ImportError` sem `rapidfuzz`** (valida a decisão AC1×AC6 das Dev Notes) — no venv limpo, `pip uninstall -y rapidfuzz` seguido de `python -c "import src.core.deduplicator"`:

   ```text
   File "...\src\core\deduplicator.py", line 5, in <module>
       from rapidfuzz import fuzz
   ModuleNotFoundError: No module named 'rapidfuzz'
   ```

   Confirma que o import morto é executado no carregamento do módulo e que omitir o pacote quebraria a tela de Duplicidades e o smoke test da Story 6.3.

3. **Escolha dos pisos de versão (risco de wheels binárias em Windows)** — consulta à API do PyPI listando a primeira release com wheel `cp314-...-win_amd64.whl`:

   | Pacote | 1ª release com wheel `cp314-win_amd64` | Piso adotado | Python exigido pelo piso |
   |---|---|---|---|
   | `numpy` | 2.3.2 | `>=2.3.2` | `>=3.11` |
   | `scikit-learn` | 1.7.2 | `>=1.7.2` | `>=3.10` |
   | `rapidfuzz` | 3.14.0 | `>=3.14.0` | `>=3.10` |
   | `pandas` | 2.3.3 | `>=2.3.3` (era `>=2.0.0`) | `>=3.9` |

   O piso efetivo de Python passa a ser **3.11** (imposto por `numpy>=2.3.2`) — daí a documentação de "Python 3.11 a 3.14" no README e o ajuste do CI.

4. **`clean_install_test` (AC4)** — venv novo criado com `C:\Python314\python.exe -m venv` (fora da árvore do projeto), `pip install -r requirements_catalog.txt`:

   ```text
   Successfully installed ... numpy-2.5.3 pandas-3.0.5 rapidfuzz-3.14.6 scikit-learn-1.9.0
   scipy-1.18.1 sqlalchemy-2.0.52 streamlit-1.63.0 dnspython-2.8.0 openpyxl-3.1.5 ... (47 pacotes)
   ```

   Todas as dependências compiladas resolveram como wheel `cp314-win_amd64` — nenhuma tentativa de build a partir do source.

5. **Navegação nas duas telas (AC4)** — `streamlit.testing.v1.AppTest` executando `src/app.py` com o interpretador do venv limpo, contra um SQLite temporário com duplicidades exatas e aproximadas:

   ```text
   [OK ] import src.core.db_manager | deduplicator | profiler | glossary | docs_br | email_val | phone | text_clean
   [OK ] boot do app (import de src/app.py)
   [OK ] conexao ao banco
   [OK ] TELA 1 - Data Discovery & Profiling  (profiling executado, "Análise concluída!")
   [OK ] TELA 2 - Analise de Duplicidades      (find_duplicates executado — TF-IDF + cosine_similarity)
         metricas: [('Registros com Duplicidade (Total no Grupo)', '2'), ('Registros Seguros (Únicos)', '3')]
   RESULTADO AC4: PASSOU — duas telas navegadas sem ImportError
   ```

6. **Servidor real (`streamlit run`) — achado bloqueante encontrado e resolvido por documentação:** subindo o servidor com `streamlit run src/app.py` a partir da raiz e abrindo uma sessão real (websocket `/_stcore/stream` + `BackMsg.rerun_script`), o app quebra:

   ```text
   File "...\src\app.py", line 5, in <module>
       from src.core.db_manager import DBManager
   ModuleNotFoundError: No module named 'src'
   ```

   Causa: `streamlit/web/bootstrap.py::_fix_sys_path` insere no `sys.path` **apenas a pasta do script** (`src/`), nunca a raiz do projeto. Não é falha de manifesto. Repetindo a mesma sessão com `python -m streamlit run src/app.py` (que coloca o diretório atual no `sys.path`), o app sobe sem nenhuma exceção e renderiza o rádio com as duas telas. O comando oficial documentado no README passou a ser `python -m streamlit run src/app.py`. **Nenhum código de aplicação foi alterado** (mexer em `src/app.py` está fora do escopo desta story).

7. **Validação do manifesto no ambiente de CI (Linux + Python 3.11)** — `pip install --dry-run --only-binary=:all: --python-version 3.11 --platform manylinux*_x86_64`: resolve integralmente com wheels (`numpy-2.4.6`, `pandas-3.0.5`, `scikit-learn-1.9.0`, `scipy-1.17.1`). Confirma que o piso 3.11 declarado é instalável e que o CI ajustado não quebra.

8. **Lint (mesma checagem do CI)** — `flake8 src --count --select=E9,F63,F7,F82`: **0** ocorrências. `npm run lint` / `npm run typecheck` são **N/A**: não existe `package.json` no projeto (aplicação Python; o gate de lint do repositório é o `flake8` do `ci.yml`).

9. **CodeRabbit** — indisponível neste ambiente (`wsl bash` → `bash: not found`). Aplicada a degradação graciosa prevista em `core-config.yaml → coderabbit_integration.graceful_degradation.skip_if_not_installed: true`; self-healing pulado, não bloqueante.

### Completion Notes List

- **[DECISÃO — AC3] `requirements.txt` foi renomeado para `requirements-aiox-tooling.txt` (via `git mv`, conteúdo preservado + cabeçalho explicativo).** Motivo: das duas alternativas oferecidas pela AC3, apenas remover o nome `requirements.txt` da raiz elimina a ambiguidade de forma mecânica, e não apenas documental — enquanto o arquivo existir com o nome padrão, quem clona o repo roda `pip install -r requirements.txt`, monta o ambiente errado e o app quebra com `ImportError`. Optou-se por **renomear em vez de apagar** porque `anthropic`/`mcp` são o tooling de IA/MCP que acompanha o repositório (uso legítimo, apenas não-do-app), e apagar destruiria informação sem necessidade. Verificado antes de decidir: nenhum processo do repositório consome `requirements.txt` — `.github/workflows/ci.yml:27` instala `requirements_catalog.txt`, e nem `anthropic` nem `mcp` são importados em qualquer ponto de `src/`. Resultado: **um único manifesto de aplicação** (`requirements_catalog.txt`), com o segundo arquivo nomeado e documentado como não-aplicação (README §2 + cabeçalho do próprio arquivo).
- **[AUTO-DECISÃO] `.github/workflows/ci.yml`: `python-version` 3.10 → 3.11.** Não é escopo novo, é consequência direta e obrigatória da Task 2: `numpy>=2.3.2` (o piso com wheel `cp314-win_amd64`) exige Python `>=3.11`, então manter o CI em 3.10 faria o `pip install -r requirements_catalog.txt` do próprio CI falhar com "No matching distribution". Rodar o CI exatamente no piso declarado (3.11) tem o efeito colateral desejável de validar continuamente a versão mínima documentada no README. Validado por dry-run de resolução (item 7 do Debug Log).
- **[AUTO-DECISÃO] Comando de execução documentado é `python -m streamlit run src/app.py`.** `streamlit run src/app.py` está quebrado por causa do `sys.path` (item 6 do Debug Log) — não por dependência faltante. Corrigir isso no código (bootstrap de `sys.path` em `src/app.py` ou empacotamento com `pyproject.toml`) alteraria código de aplicação, fora do escopo desta story; fica registrado como **débito para a Story 6.1/6.3** (a 6.3 vai querer rodar `pytest` a partir da raiz, e vai esbarrar no mesmo tema de path do pacote `src`).
- **[AUTO-DECISÃO] Piso de `pandas` elevado de `>=2.0.0` para `>=2.3.3`.** Mesma classe de risco tratada na Task 2: `pandas` tem extensão compilada e as releases 2.0.0–2.3.2 não publicam wheel `cp314-win_amd64`; um piso baixo permitiria ao resolver escolher uma versão sem wheel e tentar compilar do source no Windows. Não exclui nenhum ambiente real (o venv do projeto já roda `pandas` 3.0.5).
- **[AUTO-DECISÃO] Drivers de banco (`pyodbc`) NÃO entram no manifesto**, apesar de `src/app.py:25` sugerir `mssql+pyodbc://...` no texto de ajuda. Não há `import pyodbc` em lugar nenhum — declarar violaria a AC6. Documentado no README §1 e no rodapé do manifesto como instalação opcional, dependente do banco de destino.
- **`rapidfuzz` declarado conforme a decisão fechada nas Dev Notes** (seção "RESOLVIDO — AC1 × AC6"), com comentário no manifesto marcando-o como **pendente de revisão na Story 6.1**: se a 6.1 remover o import morto de `deduplicator.py:5`, o pacote sai do manifesto nesse momento — e não antes. O `ModuleNotFoundError` reproduzido no item 2 do Debug Log é a evidência de que a decisão está correta.
- **Relação com a Story 6.3 (registro para o quality gate de lá):** o smoke test de import de todos os módulos de `src/` (6.3 AC4) passa a ser a regressão automatizada do que esta story corrigiu. O comando exato já validado aqui está documentado no README §5 (passo 4) e roda verde no venv limpo. A 6.3 precisará resolver o mesmo tema de `sys.path`/pacote citado acima para rodar `pytest` a partir da raiz.
- **Armadilha de ambiente descoberta durante o AC4 (documentada no README §5):** em caminho de venv muito profundo, o `pip` aborta a instalação do `scikit-learn` com `OSError: [Errno 2] ... _radius_neighbors_classmode.cp314-win_amd64.lib` — é o limite de 260 caracteres do Windows, não defeito do manifesto. O teste foi refeito em caminho curto e passou.
- **Pendência humana da AC4:** a exigência de "reproduzido por outra pessoa que não a autora" **não pode ser satisfeita por um agente** — o procedimento está documentado e verificado ponta a ponta (README §5), mas a reprodução por uma segunda pessoa fica para o quality gate (@architect) confirmar.
- **Checkboxes de "Quality Gate Tasks" (seção CodeRabbit) permanecem desmarcados**: Pre-Commit/Pre-PR são atribuídos a @devops e o CodeRabbit CLI não está disponível neste ambiente (item 9 do Debug Log).
- **Nenhum arquivo de código de aplicação foi alterado.** As mudanças são: dois manifestos, um workflow de CI e um README novo.

### File List

| Arquivo | Ação | Motivo |
|---|---|---|
| `requirements_catalog.txt` | modificado | AC1/AC2/AC6 — declara `numpy>=2.3.2`, `scikit-learn>=1.7.2`, `rapidfuzz>=3.14.0`; piso de `pandas` elevado para `>=2.3.3`; comentários de rastreio (arquivo:linha) por pacote |
| `requirements.txt` → `requirements-aiox-tooling.txt` | renomeado (`git mv`) + cabeçalho | AC3 — remove a ambiguidade do nome padrão e explicita que é tooling, não app |
| `README.md` | criado | AC3/AC4/AC5 — setup (Python suportado, venv Windows/Linux, execução), qual manifesto é o do app, e o procedimento reprodutível de instalação limpa |
| `.github/workflows/ci.yml` | modificado | consequência do AC1 — `python-version` 3.10 → 3.11, o piso imposto por `numpy>=2.3.2` |
| `docs/stories/6.2.story.md` | modificado | checkboxes, Dev Agent Record, File List, Change Log, transições de status |

## QA Results

_A preencher pelo @architect (quality gate desta story) após implementação._
